# Herramientas de desarrollo: solo para el entorno local, nunca en la imagen.
# Incluye las de runtime, así que basta con instalar este archivo:
#
#   uv pip install -r requirements-dev.txt     (o: pip install -r requirements-dev.txt)

# Runtime + herramientas de pruebas (pytest, pandas, etc.)
-r requirements-test.txt

# Linter y formateador (configurados en pyproject.toml)
ruff==0.16.7

# Type checker (configurado en pyrightconfig.json)
pyright==1.1.414

# Modelos de embedding y re-ranking corriendo en proceso. Solo para local:
# permiten EMBEDDING_PROVIDER=local y RERANKER_PROVIDER=local sin depender de
# cuentas de terceros ni gastar créditos mientras se desarrolla.
sentence-transformers==6.0.1
onnxruntime==1.26.0
