# Lo mínimo para correr la suite de tests: runtime + herramientas de pruebas.
# Es lo que instala el CI. No incluye los modelos locales de requirements-dev.txt
# (sentence-transformers arrastra torch y pesa varios GB): los tests que los tocan
# los mockean.
#
#   pip install -r requirements-test.txt

-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
respx==0.23.1
aiosqlite==0.22.1

# tests/unit/test_dataset_date_shift.py y tests/matching_evaluation
pandas==3.0.5
