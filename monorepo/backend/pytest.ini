[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
pythonpath = .
markers =
    integration: requiere un Postgres real. Corre contra una base dedicada y se
        salta si el servicio no está disponible. Excluir con -m "not integration".
